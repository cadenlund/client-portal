## 2026-4-18

## Service tests & schema fixes
- Writing auth service tests (TestRegister, TestLogin)
- Each subtest sets up deps inline (q, h, s) and rolls back via transaction — no table-driven tests since inputs are loose vars, not structs
- Changed `name` column from nullable to NOT NULL in users migration, ran sqlc generate
- `CreateUserParams.Name` is plain `string` (required on creation)
- For partial updates, used `sqlc.narg(name)` in UpdateUser query — forces sqlc to generate `*string` in `UpdateUserParams` even though the column is NOT NULL, enabling optional partial updates via COALESCE
- Rule: `sqlc.narg()` = nullable in params only, column nullability is separate

- Wrote all tests for login and register service layer methods
