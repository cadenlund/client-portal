## 2026-4-12

## Auth 
- Implemented Hash & verify methods
- Originally started rolling my own based on alex edwards blog 
- [Blog](https://www.alexedwards.net/blog/how-to-hash-and-verify-passwords-with-argon2-in-go)
- He has a library for it so just use that next time
- Renamed old auth to hasher
- Renamed Auth -> Hasher, AuthConfig -> Config, NewAuth -> NewHasher
- Keep package as `auth` (umbrella for future auth primitives like tokens, sessions)
- Wrote unit tests: round-trip success, wrong password, bad version, bad variant, malformed hash
- Deleted hand-rolled errors.go, library handles its own errors now
- Renamed module to github.com/cadenlund/client-portal/apps/api

## Service layer
- Created service layer blueprint
- Could consider adding an aggergate layer to aggregate all the services since each has own deps and constructors
- Only make domain errors for mappable errors
- return wrapped errors for 500 internal server
- Implemented Register: hash -> CreateUser -> translate pg 23505 to ErrEmailTaken
- Implemented Login: GetUserByEmail -> ErrNoRows becomes ErrInvalidCredentials, verify password, mismatch also ErrInvalidCredentials (user enumeration defense)
- Created internal/domain/errors.go with ErrInvalidCredentials and ErrEmailTaken
- errors.As for typed pg errors (constraint codes), errors.Is for sentinels (pgx.ErrNoRows)
- Wrap unknown errors with fmt.Errorf("method: step: %w", err) for breadcrumb trail in logs
- Deleted service.go aggregator, will add back when 2+ services exist
- validator/v10 belongs in handler layer on DTOs, not service layer
- Session management: scs library lives in handler layer, service returns *repository.User, handler calls sm.Put()
- Fuzz testing belongs at the handler layer (bytes -> structure boundary), service tests stay deliberate
